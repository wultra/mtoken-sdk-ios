name: lint

on:
  push:
    branches:
      - develop
      - master
  pull_request:

jobs:
  pod:
    name: Pod Lib Lint
    runs-on: macos-14
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v2
      - name: Lint
        run: pod lib lint --allow-warnings